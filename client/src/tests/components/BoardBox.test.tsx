// Core
import { it, describe, expect } from 'vitest'
// Censorship object

// Others
import { renderCensorObject } from '@/tests/helper'

// Types


describe('BoardBoxComponent Suite', () => {
  // Happy cases
  it('should component runs correctly', () => {})
  it('should receive the correct props type', () => {})

  // Error cases
})
